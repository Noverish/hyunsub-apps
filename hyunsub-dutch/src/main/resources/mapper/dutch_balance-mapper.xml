<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kim.hyunsub.dutch.mapper.DutchBalanceMapper">
    <select id="select">
        SELECT *
        FROM dutch_balance
        WHERE member_id = #{memberId} AND currency = #{currency}
    </select>

    <select id="selectByMemberId">
        SELECT *
        FROM dutch_balance
        WHERE member_id = #{memberId}
    </select>

    <insert id="insert">
        INSERT INTO dutch_balance
        (member_id, currency, amount, reg_dt)
        VALUES
        (#{memberId}, #{currency}, #{amount}, NOW())
    </insert>
</mapper>
